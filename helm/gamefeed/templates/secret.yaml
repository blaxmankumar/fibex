apiVersion: v1
kind: Secret
metadata:
  name: gamefeed-secret
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "gamefeed.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ .Values.postgres.password | quote }}
  JWT_SECRET: {{ .Values.jwtSecret | quote }}
